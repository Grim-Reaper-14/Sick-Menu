# Menu Settings UI entries

Each root Menu Settings entry owns its registration/interaction glue here. Persistent settings, assets, file I/O and logging remain in `backend/system`; these files only organize frontend menu behavior.
